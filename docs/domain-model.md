# Domain Model

## Big Number

Một số nguyên không âm được biểu diễn dưới dạng chuỗi chữ số thập phân.

## Quy tắc

- Chuỗi không được rỗng.
- Chỉ chấp nhận ký tự từ `0` đến `9`.
- Không hỗ trợ số âm.
- Kết quả là chuỗi chữ số không dùng `BigInteger`.
